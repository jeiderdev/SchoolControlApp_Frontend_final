<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.schoolcontrol">

    <!-- Permisos -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:label="SchoolControl"
        android:theme="@style/Theme.App"
        android:networkSecurityConfig="@xml/network_security_config">
        <!-- android:usesCleartextTraffic="true"> -->

        <activity android:name=".ui.RegisterActivity" />
        <activity android:name=".ui.ForgotPasswordActivity" />
        <activity android:name=".ui.ResetPasswordActivity" />
        <activity android:name=".ui.MainActivity" />
        <activity android:name=".ui.ProfileActivity" />
        <activity android:name=".ui.CreateSubjectActivity" />
        <activity android:name=".ui.EditSubjectActivity"/>

        <activity android:name=".ui.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>
